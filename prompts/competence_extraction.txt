You are reviewing a single OCR-derived forensic or medical report. OCR text may be noisy, contain spelling errors, broken formatting, duplicated text, missing punctuation, incorrect line breaks, and misrecognized words. Be robust to these issues. Do not require exact wording when the meaning is clear.

## Task 1: Identify the Report Type

Decide whether the document CONTAINS a competence-to-stand-trial evaluation result anywhere in the text.

A Competence to Stand Trial report is a formal psychiatric or forensic evaluation that addresses whether the defendant is mentally capable of participating in legal proceedings. It is NOT an order, a referral, or a sanity evaluation.

Strong indicators that the document IS a Competence to Stand Trial report:
- A header or title such as "Competence to Stand Trial Report", "Competency to Stand Trial Report", "Competency Report", or similar
- Author is a physician (MD or DO); most reports are written by Michael Fuller or Stephen Baughn
- A stated reason for evaluation that is examination or evaluation of competence to stand trial
- Presence of an explicit "Opinion" or "Conclusion" section addressing competence to stand trial
- Presence of structured background history sections (birth, family, education, employment, legal, psychiatric, substance use, medical)
- A Mental Status Examination section
- A section on current or present charges
- A section explicitly labeled "Areas of Competency" or similar
- A Recommendations section
- Identification information: evaluee's name, date of birth, cause/case/criminal number
- Dates: date of examination and date of report

Not all elements need to be present. Use the overall pattern to judge intent and document type.

Important exclusion rules:
1. Do NOT classify a competence order as a competence result. An order is merely a court request for an evaluation, not the evaluation itself.
2. Do NOT classify a Sanity Evaluation as a competence result. A sanity evaluation addresses whether the defendant was mentally responsible at the time of the crime — not whether the defendant can stand trial.

---

## Task 2: Extract Report Content (only if the document IS a Competence to Stand Trial report)

If `contain_competence_result` is "yes", extract the following fields from the report. Use null for any field that is absent or cannot be determined. Be tolerant of OCR noise and formatting irregularities.

Field definitions and schemas:

- `header`: string — Title of the report exactly as it appears in the document (preserve original wording, 1 line)
- `author`: string — Full name and credentials of the report author; format as "First Last, Credential" (e.g., "Michael Fuller, M.D.")
- `date_of_examination`: string — Date when the interview or examination took place; format as "YYYY-MM-DD"; use "YYYY-MM" or "YYYY" if full date is not available; null if absent
- `date_of_report`: string — Date when the report was submitted or signed; same format rules as `date_of_examination`
- `cause_number`: string — Cause, case, or criminal number exactly as it appears in the document (e.g., "12-CR-12345"); null if absent
- `evaluee_name`: string — Full name normalized to "LAST, FIRST MIDDLE" format (e.g., "DOE, JOHN MICHAEL")
- `evaluee_dob`: string — Date of birth; format as "YYYY-MM-DD"; use "YYYY-MM" or "YYYY" if full date is not available; null if absent
- `reason_for_evaluation`: string — One sentence stating why the evaluation was requested (e.g., "Court-ordered competency evaluation for criminal proceedings")
- `current_charges`: array of strings — Criminal charge(s); each as a concise label (e.g., ["Murder", "Aggravated Assault"]); null if absent
- `birth_place`: string — City and state/country where the evaluee was born; format as "City, State, Country" (e.g., "Galveston, TX, USA"); null if absent
- `family`: string — Family composition and upbringing; format as a brief structured summary listing key members and relationships (e.g., "Born to [mother] and [father]; has N siblings; raised by [guardian]"); null if absent
- `childhood_development`: string — 1–2 sentence summary of notable developmental history; null if absent or unremarkable
- `education`: string — Highest level of education completed; format as "Grade or Degree, School or District if known" (e.g., "10th grade, Galveston ISD" or "GED"); null if absent
- `employment`: array of strings — Employment history; each entry as "Job Title, Employer (dates or period)" (e.g., ["Cashier, Walmart (2010–2015)"]); null if absent
- `legal_history`: array of strings — Prior legal involvement; each entry as "Charge/Offense, Jurisdiction, Year or period" (e.g., ["Theft, Galveston County, 2012"]); null if absent
- `military_history`: string — Military service summary; format as "Branch, approximate service dates, discharge status" (e.g., "U.S. Army, 2005–2009, Honorable Discharge"); null if absent or no military service
- `psychiatric_history`: array of strings — Prior psychiatric diagnoses and significant treatment history; each entry as "Diagnosis or event, approximate date or period" (e.g., ["Major Depressive Disorder, diagnosed 2010", "Inpatient hospitalization, 2015"]); null if absent
- `substance_use`: array of strings — Substance use history; each entry as "Substance, usage pattern or history" (e.g., ["Alcohol, daily use since age 18", "Marijuana, occasional use"]); null if absent
- `medical_history`: array of strings — Significant medical conditions; each entry as "Condition or procedure, approximate date if known" (e.g., ["Type 2 Diabetes, diagnosed 2018", "Appendectomy, 2005"]); null if absent
- `mental_status_examination`: string — Concise structured summary covering key domains: appearance, behavior, speech, mood, affect, thought process, thought content, cognition, insight, judgment (1–3 sentences per domain if notable, otherwise omit); null if absent
- `areas_of_competency`: string — Concise summary of competency domains assessed and findings; format as a brief list or 2–3 sentences (e.g., "Understands nature of charges: yes; Understands court proceedings: yes; Ability to assist counsel: impaired"); null if absent
- `opinion`: string — One to two sentences stating the clinician's competency determination (e.g., "Evaluee is competent to stand trial" or "Evaluee is not competent to stand trial due to [reason]")
- `recommendations`: string — Concise list or 1–2 sentences summarizing recommendations (e.g., "Inpatient psychiatric treatment to restore competency"); null if absent

---

## Return Format

Return a single valid JSON object with no trailing commas. If `contain_competence_result` is "no", omit all extraction fields.

{
    "contain_competence_result": "yes" | "no",
    "header": string | null,
    "author": string | null,
    "date_of_examination": "YYYY-MM-DD" | "YYYY-MM" | "YYYY" | null,
    "date_of_report": "YYYY-MM-DD" | "YYYY-MM" | "YYYY" | null,
    "cause_number": string | null,
    "evaluee_name": string | null,
    "evaluee_dob": "YYYY-MM-DD" | "YYYY-MM" | "YYYY" | null,
    "reason_for_evaluation": string | null,
    "current_charges": [string, ...] | null,
    "birth_place": "City, State, Country" | null,
    "family": string | null,
    "childhood_development": string | null,
    "education": string | null,
    "employment": [string, ...] | null,
    "legal_history": [string, ...] | null,
    "military_history": string | null,
    "psychiatric_history": [string, ...] | null,
    "substance_use": [string, ...] | null,
    "medical_history": [string, ...] | null,
    "mental_status_examination": string | null,
    "areas_of_competency": string | null,
    "opinion": string | null,
    "recommendations": string | null
}

Now analyze this OCR report:

{{report}}